{
  "name": "blueprint",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "./node_modules/.bin/tsc",
    "copy-dependencies": "cp -r node_modules/ build/node_modules",
    "clean": "rm -r build/ && rm -r node_modules/"
  },
  "devDependencies": {
    "aws-sdk": "^2.202.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "lodash": "^4.17.5"
  }
}
